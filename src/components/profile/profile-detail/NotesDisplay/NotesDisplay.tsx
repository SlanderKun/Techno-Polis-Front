import React from 'react';
import {observer} from "mobx-react-lite";
// import ProfileSection from "../ProfileSection/ProfileSection.tsx";

const NotesDisplay:React.FC = () => {
    return (
        <>
            {
                // <ProfileSection>
                //
                // </ProfileSection>
            }
        </>
    );
};

export default observer(NotesDisplay);