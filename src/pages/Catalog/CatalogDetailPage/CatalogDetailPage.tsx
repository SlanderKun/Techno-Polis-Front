import React from 'react';
import {observer} from "mobx-react-lite";

const CatalogDetailPage:React.FC = () => {
    return (
        <div>

        </div>
    );
};

export default observer(CatalogDetailPage);