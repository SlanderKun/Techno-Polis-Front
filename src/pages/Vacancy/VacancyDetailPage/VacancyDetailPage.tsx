import React from 'react';
import {observer} from "mobx-react-lite";

const VacancyDetailPage:React.FC = () => {
    return (
        <div>

        </div>
    );
};

export default observer(VacancyDetailPage);