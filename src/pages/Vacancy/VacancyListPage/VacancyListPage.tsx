import React from 'react';
import {observer} from "mobx-react-lite";

const VacancyListPage:React.FC = () => {
    return (
        <div>

        </div>
    );
};

export default observer(VacancyListPage);